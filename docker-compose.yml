version: '3.8'

services:
  HomeInmobiliaria:
    build: .
    container_name: home-inmobiliaria
    ports:
      - "80:80"
    networks:
      - frontend
    restart: unless-stopped

networks:
  frontend:
    external: true
