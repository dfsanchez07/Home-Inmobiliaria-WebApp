version: '3.8'

services:
  homeinmobiliaria:
    build: .
    container_name: home-inmobiliaria
    expose:
      - "80"
    networks:
      - frontend
    restart: unless-stopped

networks:
  frontend:
    external: true
